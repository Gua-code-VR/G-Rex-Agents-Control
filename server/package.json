{
  "name": "g-rex-agent-control-server",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Control plane di G-Rex Agent Control: API REST + registro progetti e stato + stato Git essenziale + persistenza SQLite locale (node:sqlite, SQLite nativo di Node.js)",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "fastify": "^5.2.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=23.4"
  }
}
